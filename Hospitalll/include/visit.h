#ifndef VISIT_H
#define VISIT_H

typedef enum {
    VISIT_TYPE_GENERAL,
    VISIT_TYPE_CONSULTATION,
    // Add more types as needed
} VisitType;

//extern const char *const visit_type_names[];

#endif // VISIT_H
